// import {GET_SALONS} from '../actions/types'

// let intialState = {data: [], isLoading: true};

// const getSalonReducer = (state=intialState, action) =>{
//     switch (action.type) {
//         case GET_SALONS:
//             state = Object.assign({}, state, {data: action.data, isLoading: false}) 
//     }
// }

